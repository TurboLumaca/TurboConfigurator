{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core desktop capability for the main workspace window.",
  "windows": ["main"],
  "platforms": ["macOS"],
  "permissions": [
    "core:default",
    "opener:default",
    "dialog:default",
    "fs:default",
    "shell:default",
    "store:default"
  ]
}
